import styled from "styled-components";

export const Wrapper = styled.main`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  padding: 7.5rem 2rem;
  width: 100%;

  .left-side {
    display: flex;
    flex-wrap: wrap;
  }
`;
