import styled from "styled-components";

export const Wrapper = styled.main`
  display: flex;
  max-width: 185rem;
  padding: 5rem 0rem 5rem 0rem;
  margin: 0 auto;

  .left {
    display: flex;
    flex-wrap: wrap;
    width: 100%;
  }
`;
